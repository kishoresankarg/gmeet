global:
  scrape_interval: 5s

scrape_configs:
  - job_name: "backend"
    metrics_path: /health
    static_configs:
      - targets: ["host.docker.internal:8000"]

  - job_name: "jenkins"
    metrics_path: /
    static_configs:
      - targets: ["host.docker.internal:8080"]